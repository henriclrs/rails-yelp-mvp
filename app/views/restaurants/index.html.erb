<div class="container text-center">
  <%= link_to "Add a new restaurant", new_restaurant_path %>

  <% @restaurants.each do |restaurant|%>
  <h2><%= link_to restaurant.name, restaurant_path(restaurant) %></h2>
  <p>@reviews.restaurant</p>
  <% end %>
</div>
