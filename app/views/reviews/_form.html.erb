<div class="container">
  <%= simple_form_for [@restaurant, @review = Review.new] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit "add a review", class: "btn btn-primary" %>
  <% end %>
</div>
